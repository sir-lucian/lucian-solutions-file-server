server {
    listen 80;
    server_name _;

    location /files/ {
        alias /usr/share/nginx/html/files/;
        autoindex on;
        try_files $uri $uri/ =404;
    }

    location / {
        return 403;
    }
}
