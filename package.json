{
  "name": "readonly-file-server",
  "version": "1.0.0",
  "description": "Read-only file storage server (Dockerized, TypeScript)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sharp": "^0.34.5",
    "ffmpeg-static": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.0",
    "typescript": "^5.0.0"
  }
}
